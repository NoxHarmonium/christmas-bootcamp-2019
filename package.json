{
  "name": "christmas-bootcamp-2019",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "pushd client && yarn install && yarn build && popd && pushd server && yarn install && yarn build && popd"
  }
}
