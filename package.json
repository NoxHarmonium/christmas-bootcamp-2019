{
  "name": "christmas-bootcamp-2019",
  "version": "1.0.0",
  "scripts": {
    "install-server": "cd server && yarn install --production=false && yarn build && cd ..",
    "install-client": "cd client && yarn install --production=false && yarn build && cd ..",
    "postinstall": "env && run-p install-client install-server"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
