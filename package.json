{
  "name": "christmas-bootcamp-2019",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "yarn --cwd client install && yarn --cwd client build && yarn --cwd server install && yarn --cwd server build"
  }
}
